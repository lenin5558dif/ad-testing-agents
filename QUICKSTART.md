# 🚀 Quick Start Guide

Быстрый старт за 3 минуты.

## Шаг 1: Установка (1 мин)

```bash
cd experiments/ad-testing-agents

# Создать виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate  # macOS/Linux
# или: .venv\Scripts\activate  # Windows

# Установить зависимости
pip install -e .
```

## Шаг 2: Настройка API ключа (30 сек)

```bash
# Скопировать пример
cp .env.example .env

# Открыть .env и добавить свой API ключ Anthropic
# ANTHROPIC_API_KEY=sk-ant-your-key-here
```

Где взять API ключ:
- Перейти на https://console.anthropic.com/
- Создать новый API key
- Скопировать в .env

## Шаг 3: Тест установки (опционально)

```bash
python scripts/test_basic.py
```

Ожидаемый вывод:
```
✅ Loaded 8 personas
✅ Received 2 responses
✅ All tests passed!
```

## Шаг 4: Запуск dashboard (30 сек)

```bash
streamlit run dashboard/app.py
```

Откроется браузер: http://localhost:8501

## Первый тест

1. В левой панели выберите персоны (по умолчанию все выбраны)
2. Введите тестовый оффер:
   ```
   Заголовок: Лазерная эпиляция — первая процедура 990₽
   Текст: Забудьте о бритье навсегда. Безболезненно, быстро, гарантия результата.
   CTA: Записаться на процедуру
   ```
3. Нажмите "🚀 Протестировать оффер"
4. Через 5-10 секунд увидите ответы от всех персон

## Что дальше?

- Читайте [README.md](README.md) для полной документации
- Тестируйте разные офферы
- Анализируйте какие сегменты конвертируются лучше
- Экспериментируйте с персонами

## Troubleshooting

**Ошибка "ANTHROPIC_API_KEY not set"**
→ Проверьте что в .env есть ключ и нет опечаток

**Медленно работает**
→ В .env установите `BATCH_PARALLEL=true`

**Ошибки импорта**
→ Убедитесь что выполнили `pip install -e .`

---

💡 **Совет**: Начните с 2-3 персон для экономии API calls, затем масштабируйте на всех 8.
